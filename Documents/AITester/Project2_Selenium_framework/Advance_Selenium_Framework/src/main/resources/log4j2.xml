<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="warn">
    <Properties>
        <Property name="log.dir">logs</Property>
        <Property name="log.pattern">%d{yyyy-MM-dd HH:mm:ss} [%-5p] [%t] %c{1} - %m%n</Property>
    </Properties>

    <Appenders>
        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${log.pattern}" />
        </Console>

        <!-- File Appender for all logs -->
        <File name="File" fileName="${log.dir}/application.log">
            <PatternLayout pattern="${log.pattern}" />
        </File>

        <!-- File Appender for error logs -->
        <File name="ErrorFile" fileName="${log.dir}/error.log">
            <PatternLayout pattern="${log.pattern}" />
            <Filters>
                <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY" />
            </Filters>
        </File>

        <!-- Rolling File Appender -->
        <RollingFile name="RollingFile" fileName="${log.dir}/app.log"
                     filePattern="${log.dir}/app-%d{yyyy-MM-dd}-%i.log">
            <PatternLayout pattern="${log.pattern}" />
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true" />
                <SizeBasedTriggeringPolicy size="10MB" />
            </Policies>
            <DefaultRolloverStrategy max="10" />
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Application Loggers -->
        <Logger name="com.crm.automation" level="INFO" additivity="false">
            <AppenderRef ref="Console" />
            <AppenderRef ref="RollingFile" />
            <AppenderRef ref="ErrorFile" />
        </Logger>

        <!-- Selenium Logger -->
        <Logger name="org.openqa.selenium" level="WARN" additivity="false">
            <AppenderRef ref="Console" />
        </Logger>

        <!-- Root Logger -->
        <Root level="INFO" additivity="true">
            <AppenderRef ref="Console" />
            <AppenderRef ref="RollingFile" />
        </Root>
    </Loggers>
</Configuration>
