[pytest]
# Pydantic model names like TestCase/TestPlan/TestCaseSuite match pytest's
# default "Test*" class-collection pattern; this project has no class-based
# tests, so disable that pattern instead of renaming domain models.
python_classes = NoClassBasedTests
